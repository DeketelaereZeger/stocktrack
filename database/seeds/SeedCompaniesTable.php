<?php

use Illuminate\Database\Seeder;
use Carbon\Carbon;

class SeedCompaniesTable extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        //After some thinking i noticed it would be less time consuming to just seed all the data from one company in the different table in one api call, which makes this document obsolete
    }
}
